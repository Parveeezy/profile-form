import React from 'react';
import { CheckboxComp } from './components';

const CheckboxUI = () => {
    return (
        <div>
            <CheckboxComp />
        </div>
    );
};

export default CheckboxUI;